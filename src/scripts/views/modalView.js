define(['backbone', 'underscore', 'mousetrap'], function (Backbone, _, Mousetrap) {
    'use strict';

    var modalViewStack = [];

    var ModalView = Backbone.View.extend(

    );

    return ModalView;
});